# gstack

Use the `/browse` skill from gstack for all web browsing. Never use `mcp__claude-in-chrome__*` tools.

Available gstack skills:
- `/office-hours` — structured async help session
- `/plan-ceo-review` — CEO-perspective plan review
- `/plan-eng-review` — engineering plan review
- `/plan-design-review` — designer's eye plan review
- `/design-consultation` — design direction consultation
- `/design-shotgun` — generate multiple design variants for comparison
- `/design-html` — finalize designs into production HTML/CSS
- `/review` — code review of current diff
- `/ship` — full ship flow (review → merge → deploy)
- `/land-and-deploy` — land PR and deploy
- `/canary` — canary deploy
- `/benchmark` — performance regression detection
- `/browse` — browse the web using the browser daemon
- `/connect-chrome` — connect to Chrome browser
- `/qa` — full QA pass
- `/qa-only` — QA without other steps
- `/design-review` — live site visual audit
- `/setup-browser-cookies` — set up browser cookies
- `/setup-deploy` — configure deploy pipeline
- `/setup-gbrain` — configure gbrain
- `/retro` — retrospective
- `/investigate` — investigate a bug or issue
- `/document-release` — generate release notes
- `/document-generate` — generate documentation
- `/codex` — run Codex agent
- `/cso` — chief security officer review
- `/autoplan` — auto-generate an implementation plan
- `/plan-devex-review` — developer experience plan review
- `/devex-review` — developer experience review
- `/careful` — careful mode for risky operations
- `/freeze` — freeze deploys
- `/guard` — guard against accidental deploys
- `/unfreeze` — unfreeze deploys
- `/gstack-upgrade` — upgrade gstack
- `/learn` — manage project learnings
